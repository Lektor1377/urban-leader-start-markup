# Urban Leader Start — Markup

This repository contains the frontend markup (HTML, TailwindCSS and Web Components)  
for the Urban Leader Start project.  
It is used as a standalone markup environment and also as a submodule  
in the main Bitrix project.

---

##   Running locally

### 1. Install packages
```bash
npm install
